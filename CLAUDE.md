# CLAUDE.md — рабочий журнал и навигация

> Заполняется ассистентом: сюда сразу пишем новую информацию, что сделали и что
> планируем. Раздел «Журнал» ведём для отчётности после каждого значимого ответа.

## Точки входа в проект (читать в этом порядке)

1. [ONBOARDING.md](ONBOARDING.md) — лучший обзор: раскладка репо, архитектура фронта,
   разобранные грабли, деплой. Начинать отсюда.
2. [gymassistant/miniapp/README.md](gymassistant/miniapp/README.md) — «почему гибрид»,
   почему таймер отдыха живёт в БД, а не в браузере.

## Что за проект (одним абзацем)

Telegram-бот (aiogram 3, поллинг через SOCKS5) + Mini App (FastAPI, ES-модули без
сборки) в одном репозитории. Общая БД (PostgreSQL/SQLAlchemy async), общий слой
данных `database/orm_query.py`. Роли строго разделены: **Mini App** — весь UI
(тренировка, программы, каталог, история, графики), **бот** — точка входа и
**единственный, кто умеет уведомлять** (пинги отдыха, `workers/rest_notifier.py`).

## Незыблемые правила

- Пинг отдыха — **новое сообщение с удалением предыдущего**, никогда не
  `editMessageText` (edit не даёт ни пуша, ни вибрации → убивает весь смысл).
- Состояние тренировки **не хранится между запросами**: план = упражнения дня,
  факт = записанные подходы, текущий шаг = первое расхождение.
- id приходят с фронта — им нельзя верить, проверять владение через `ownership.py`.
- Даты хранятся naive-UTC, раскладываются по календарю в поясе пользователя
  (заголовок `X-Timezone`).

## Ключевые команды

```bash
# тесты (из каталога gymassistant/)
./.venv-bot/bin/pytest tests/ -q

# локальный запуск Mini App
DB_URL=sqlite+aiosqlite:///./demo.db MINIAPP_BOT_TOKEN=<токен> \
  ./miniapp/.venv/bin/uvicorn miniapp.main:app --port 8099
```

## Текущее состояние (2026-08-15)

Всё накопленное уходит на прод этим пушем. До него прод стоял на `*-prod-4d5a40c`,
и ровно там же стоял `origin/main`: четыре коммита (`4e29ad3`…`decc221` — сборка
программы новичком, шаблоны, поиск по каталогу, перетаскивание) лежали
**только локально и никогда не пушились**, поэтому CI их и не выкатывал.
Отставание прода — это всегда отставание `origin/main`: деплоит push, а не
локальный коммит.

Тестовый контур монтирует рабочий каталог с диска и потому всегда впереди прода;
правки Python требуют `kubectl rollout restart deploy/gym-miniapp-test -n gym-prod`.
**Прод и тест — на разных базах:** прод на `postgres` (`bot-secrets/DB_URL`,
nodeport 30432), тест на `gym-miniapp-db` (`miniapp-testdb-secrets`, только
ClusterIP). Миграцию надо катить в обе, и тестовая забывается легче.

В проде сейчас: Material 3, нижняя навигация из 3 кнопок, пропуск подхода,
громкие пинги отдыха, пропущенный день и недельная карточка, квартильный heatmap,
readinessProbe у бота и миграции Job'ом в CI. ML-стека (`press_api/`, `predictor.py`,
`neiro_api.py`) нет ни в репо, ни в кластере. Кнопка меню ведёт на
`https://sk1bid.ru/gym/?v=4`.

Рядом с тренировками теперь лежат метрики Apple Health (`health_metric`), их
раз в сутки переносит CronJob `gym-health-sync` из соседнего стека
`health-export`. Смотрит на всё Grafana на `grafana.sk1bid.ru`.

Тестов 99, все зелёные: `./.venv-bot/bin/pytest tests/ -q` из каталога `gymassistant/`.

Каталог пресетов приведён в порядок 19 августа (см. журнал): `miniapp/seed.py` —
единственный источник правды об имени, категории и снаряде, а живая база
догоняется при старте. Выкачено 21 августа.

У пользователя 851690283 с 19 августа активна программа 6 «Гипертрофия»
(верх/низ, Вт/Чт/Сб/Вс), «Фулбади» (id 1) лежит рядом нетронутой. Состав правился
21 августа скриптом `gymassistant/scripts/program_v2.sql`.

## Открытые задачи / бэклог (из ONBOARDING §10)

- [ ] Свайп реализован только для touch — мышью на десктопе не работает.
- [ ] Числа в календаре активности лежат в `title=""` — на телефоне не видны, тапа
      по квадрату нет.
- [ ] nginx `location /gym-test/` правлен только на сервере; в репо расходящаяся копия.
- [ ] Timing-middleware в `miniapp/main.py` включён постоянно — убрать, если не нужен.
- [ ] Светлая тема вживую проверялась меньше тёмной.
- [ ] Живой `gym-miniapp-test` отстал от манифеста (лишний env `NEIRO_API_URL`).
- [ ] Снаряд и подсказка по весу глазами не проверены — тест-под с ними
      крашился трое суток, первый успешный старт 15 августа.

## Журнал

- **2026-07-26** — Изучил репозиторий, завёл этот CLAUDE.md.
- **2026-07-26** — Редизайн главного экрана (`js/screens/home.js` + `styles.css`).
  Проблемы, которые чинили: зелёный акцент был на 4 разных сущностях; глифы `↻`/`·`/`›`
  вместо иконок; «следующее» оторвано от списка упражнений; два конкурирующих CTA
  («Начать» + «Отработать»); разный формат подходов («3 подхода по 10» vs «3×10»).
  Сделано:
  - Инлайновые SVG-иконки (стиль навигации: 24×24, stroke 1.8) — объект `ICON` в
    home.js; `↻` → приглушённый значок «повтор» только у круговых.
  - Упражнения дня одним блоком `.session`; ближайшее — выделенная первая строка
    (`.ex-line.next` с чипом `primary-container`), а не отдельная акцентная карточка.
  - Насыщенный `primary` остался ровно на одной кнопке. «Пропущено» — тихая строка
    `.missed-row` с текстовым действием вместо второго CTA.
  - Единый формат подходов «N × M» (функция `sets()`).
  - Осталось глазами проверить в Telegram (светлая/тёмная), особенно активную
    тренировку и день отдыха. Глиф `›` в шторке выбора дня (`.list-item .chev`) не
    трогал — общий компонент.
- **2026-07-26** — Доработки главной по фидбеку:
  - **Шторка (`ui.js` `sheet()`) теперь стягивается вниз** — та же критически
    затухающая пружина + прогноз инерции, что в свайпе разделов (константы
    продублированы в ui.js). Тянется только от верха (при `scrollTop === 0`), тап
    мимо тоже уезжает с доводкой. Экспорт `close` остался мгновенным (зовётся перед
    переходом на другой экран). Общий компонент — используется 8 экранами.
  - **«Тренировать другой день»** → `.btn.outlined` + иконка календаря (раньше
    `.btn.quiet` не читалась как кнопка).
  - **Недельная карточка внизу главной** (закрывает пустое место): кольцо цели
    недели (done/goal, goal = число тренировочных дней в программе) + серия недель
    тихой строкой с огоньком, показывается от 2 недель; при закрытой цели кольцо
    с галочкой, серия в акценте. Функция `weeklyCard()` в home.js, стили `.week-card`.
  - **Сервер**: `/api/bootstrap` отдаёт `week: {done, goal, streak}` — новая
    `weekly_progress()` в `routers/schedule.py` (переиспользует
    `orm_get_sessions_summary`). Серия мягкая: грейс на текущую неделю, считается по
    поясу клиента.
  - Стягивание шторки проверено вживую в тест-боте — работает.
  - Добавлены 3 теста на `week` в bootstrap (`test_weekly_*` в test_miniapp_api.py):
    цель/сделано, серия из подряд идущих недель, мягкая серия (грейс + сброс на
    пропущенной неделе). Хелпер `_backdate()` двигает сессию по датам. Тесты 42/42.

- **2026-07-26** — Шкала heatmap активности переведена на **квартили** (как у GitHub),
  `heatmap()` в `js/screens/profile.js`. Было: доля от максимума (`sets/max_sets`,
  пороги .25/.5/.75) — один марафонский день прижимал типичные к бледному уровню.
  Стало: пороги p25/p50/p75 по дням С ТРЕНИРОВКАМИ (нули — фон, в расчёт не идут),
  цвет = ранг дня среди своих же. Отдельная ветка на нулевой разброс (все тренировки
  одинаковы → ровный уровень 3, иначе всё село бы в первый квартиль). `max_sets`
  клиентом больше не используется, на сервере остался справочной величиной —
  комментарий в `routers/profile.py` поправлен. Тесты 42/42.

- **2026-07-26** — Разбор экрана подхода по скриншоту и правки (`js/screens/workout.js`
  + `styles.css`). Найденные проблемы и что сделано:
  - **Кнопка врала.** «Пропустить подход» открывала шторку, первый пункт которой
    назывался так же. Теперь многоточие обещает выбор (`skipLabel()`), а на последнем
    подходе упражнения выбора нет — пропуск идёт сразу, без шторки. Шторка
    переименована: «Пропустить» → «Только этот подход» / «Всё упражнение · N».
    Слова склоняются по типу блока (`words()`): у круговых это «круг», потому что
    `set_number` кругового и есть номер круга.
  - **Дыра в треть экрана + прыгающая вёрстка.** Вместо списка «Записано» (пустого
    до первого подхода) — **таблица всех подходов упражнения** (`setTable()`) над
    степперами. Строки нарисованы сразу на все `total_sets`, поэтому запись подхода
    не двигает степперы под пальцем, а сами степперы уехали из верхней трети ближе
    к большому пальцу. Текущая строка помечена пилюлей `.pill.on`, будущие показывают
    «было 40 × 10», записанные — результат и правятся тапом.
  - **«прошлый раз» дублировал степперы.** Поля предзаполнены ровно прошлым разом,
    так что строка наверху всегда повторяла оба числа. Убрана; справка живёт
    в таблице на строках, которых ещё не было. В `.meta` остался только рекорд.
  - **Два счётчика подходов рядом** («подход 1 из 3» и «осталось 12») — уровни
    разведены: шапка про упражнение, `planFold()` про тренировку («ещё N подходов»
    в строке раскрывашки). **Полоса прогресса удалена совсем** — 3 px, залитые
    на ноль, были неотличимы от разделителя.
  - **«ПЛАН» не читался кнопкой** (главная претензия). Был набран как `.section-title`
    (11 px, капитель, outline) — то есть стилем ПОДПИСИ. Стал рядом-контролом: 48 px
    высоты, текст 15/500 основным цветом, пояснение справа, SVG-шеврон с поворотом
    на 180°, подсветка на нажатии, волосяные линии — язык `.list-item`.
  - **Зелёный ушёл с единственной кнопки.** `.btn.outlined` покрашена нейтрально
    (`--on-surface` вместо `--primary`) — глобально, значит чинится заодно «Тренировать
    другой день» на главной и «+1 минута» в отдыхе. `.plan-item .mark` тоже сняли
    с `--primary` (акцентом красилась даже «·» будущего шага).
  - **Степперы разъезжались**: пустой `.delta` был только под весом. Теперь слот
    у обоих (+ добавлена дельта повторений), отступы симметричны. Подписи полей:
    «КГ»/«ПОВТОРЕНИЙ» → «ВЕС, КГ»/«ПОВТОРЕНИЯ».
  - **Число не выглядело вводимым** — под ним активная линия filled-поля M3:
    волосяная в покое, `--primary` 2 px под фокусом.
  - **`.btn.quiet`** («Закончить тренировку») — `width: fit-content`: кликалась
    во всю ширину, включая пустоту по краям.
  - Мёртвый CSS убран: `.progress`, `.progress-row`, `.tiles`/`.tile`,
    `.set-chip.skipped`. `.set-chip` оставлен — им пользуется история в профиле.
  - Тесты 42/42 (изменения чисто фронтовые). **Глазами не проверено** — браузера
    на машине нет; тест-под монтирует каталог через hostPath, так что достаточно
    обновить страницу в `@sk1bid_test_bot`.

- **2026-07-31** — Перенёс всё с тестового контура на прод.
  - Незакоммиченное дерево разложено на 6 коммитов: фичи бэка (`set.skipped`,
    громкие пинги, пропущенный день, недельный прогресс) → снос ML-стека →
    фронт-редизайн → падение на старте при отставшей схеме → доки → `?v=4`.
    Порядок не случаен: фичи идут ПЕРЕД сносом ML, иначе коммит с удалением
    `services/predictor.py` оставил бы `state.py` с мёртвым импортом.
  - `main` ← `--no-ff` мерж (`aecce59`), локальный чекаут переведён на `main`:
    CI на сервере делает `git merge --ff-only origin/main` в этом же каталоге.
  - Миграции с хоста через `postgres-nodeport` (30432) — из прод-пода нельзя,
    в его образе новых файлов миграций нет. Команда записана в ONBOARDING §9.
  - **Грабли:** `kubectl rollout status` соврал. У `gym-bot` нет readinessProbe,
    поэтому упавший через 2 секунды под успел засчитаться доступным, погасить
    старый и отрапортовать успех — прод-бот лежал до накатки миграций. У
    `gym-miniapp` проба `/healthz` есть, там старый под пережил окно. Записано
    в ONBOARDING §10.
  - Опасение из ONBOARDING про `ghcr.mirror.lcr.io` не подтвердилось: мирор
    действительно не резолвится, но containerd уходит в сам `ghcr.io` и образы
    тянет. Формулировка в §5 поправлена.

- **2026-07-31** — Починил CI/CD и недельную карточку.
  - **Деплой падал с `Run Command Timeout`** — без единого слова о причине. Причина
    была та же ненакатанная схема: поды в CrashLoopBackOff, `rollout status` ждёт их
    без ограничения, ssh-action прибивает шаг. Сделано: миграции катит сам CI
    Job'ом (`k8s/migrate-job.yaml`) на только что собранном образе и ДО смены
    образов подов; опрос идёт и на `Complete`, и на `Failed` (`kubectl wait
    --for=condition=complete` на упавшем job'е высиживает весь таймаут); у каждого
    ожидания свой `--timeout`, при неудаче в лог CI летят логи пода и `describe`;
    `command_timeout: 20m` на ssh-action как внешняя страховка. Убран
    `rollout restart` — `TAG` содержит sha, `set image` и так катит поды, а лишний
    restart запускал вторую волну.
  - **Боту добавлена readinessProbe** и под неё `/healthz` в режиме поллинга
    (`start_health_server()` в app.py, флаг `_ready` поднимается последним шагом
    `on_startup_polling`). Проверено локально: 503 до готовности, 200 после.
  - **Недельная карточка врала в конце недели**: «ещё N тренировок» было разностью
    цели и сделанного, поэтому в пятницу с 0/4 просила четыре тренировки за два
    оставшихся дня. Сервер теперь отдаёт `week.left` — сколько тренировочных дней
    ПРОГРАММЫ на этой неделе ещё впереди (сегодняшний считается, пока не отработан),
    и просьба ограничена им; когда таких дней нет — «План недели позади». Грейс
    серии показан словами: «Серия: 6 недель · держится до конца недели» при нуле
    сделанных, иначе 0/4 рядом с шестёркой читалось как противоречие.
    Календарную неделю Пн→Вс оставил: так же считают Fitbit (цель «N дней в неделю»)
    и Apple (Perfect Week, Fitness+ weekly streak), а скользящее окно разошлось бы
    с нашей же понедельной серией. +2 теста (`test_weekly_left_*`), итого 44.
  - **Первый прогон нового пайплайна вскрыл два бага в нём же** (`f5a78c6`):
    - опрос Job'а спрашивал «какие условия истинны» и сравнивал ответ с `Complete`,
      а завершённый Job несёт ещё и `SuccessCriteriaMet` — jsonpath отдавал
      `SuccessCriteriaMet Complete`, цикл не выходил, и успешная миграция уронила бы
      деплой через 5 минут опроса. Теперь каждое условие читается по имени;
    - `kubectl apply` возвращал в спеку плавающий тег `:bot-prod`, а при
      `imagePullPolicy: IfNotPresent` узел брал закэшированный старый слой и начинал
      выкатку на нём ещё до `set image`. Тег подставляется прямо в манифест
      (`sed`), apply сам стал деплоем, `set image` убран.
    - **Проба при этом отработала как надо**: под со старым образом не стал готовым,
      живой бот не погас. Итог прогона: Job `Complete` за 12 с, оба деплоймента на
      `*-prod-f5a78c6`, поды 2/2 и 1/1.
    - `access_log=None` у health-сервера: kubelet писал строку об успешной пробе
      каждые 10 секунд (~8600 строк в сутки).

- **2026-08-02** — Главная врала про идущую тренировку. Два симптома, одна причина:
  про тренировку знала ТОЛЬКО ветка «сегодня есть упражнения» в `todayBlock()`
  (`home.js`), а тренировать можно любой день программы — пропущенный отрабатывают
  кнопкой, любой другой выбирают шторкой. Отсюда: (1) в день отдыха экран уходил
  в ветку «упражнений не запланировано» и о тренировке молчал — вернуться в неё
  с главной было нельзя вовсе, в навигации экрана тренировки нет, оставалось
  закрыть и открыть приложение ради восстановления в `main.js`; (2) в обычный день
  заголовок, счётчик подходов и список упражнений оставались СЕГОДНЯШНИМИ, хотя
  отрабатывалась суббота. Сервер был ни при чём — `active_session` отдавался верно.
  Сделано:
  - `active_session` (строка-id) → объект `active: {session_id, day: {…, exercises},
    done, total, next_exercise_id}`; `active_training()` в `routers/schedule.py`.
    Упражнения бесплатны — они уже загружены ради `build_plan`; текущий шаг считает
    тот же `current_step`, что и экран тренировки.
  - `activeBlock()` в `home.js`: пока тренировка идёт, она забирает экран целиком —
    её день в заголовке, её упражнения в списке. Проверка стоит ПЕРЕД всеми
    остальными ветками, включая «нет программы». Сегодняшнее число не теряется:
    уходит в подпись («3 из 12 подходов · сегодня воскресенье»), когда день не тот.
  - `sessionCard(exercises, currentId)` сведена к одному правилу: выделена строка,
    которую делать сейчас. До старта это первое упражнение, в идущей тренировке —
    шаг с сервера (третье по списку — обычное дело), `null` — выделять нечего.
    Прежняя ветка «в идущей тренировке следующего нет» ссылалась на `resumeCard`,
    который его не называл; сам `resumeCard` удалён.
  - Кнопки, начинающие ДРУГУЮ тренировку, на этом экране больше не показываются:
    старт другого дня молча закрывает текущую (`orm_finish_training_session`
    в `/training/start`).
  - +3 теста (`test_active_training_*`), итого 47. Проверить глазами в тест-боте:
    правки Python требуют `kubectl rollout restart deploy/gym-miniapp-test`.

- **2026-08-10** — Три жалобы по скриншотам с прода, три разные причины.
  - **Серия врала.** «Серия: 8 недель» рядом с кольцом 0/4 — неделя засчитывалась,
    если в ней была ХОТЬ ОДНА тренировка. Теперь в серию идут только недели, где
    цель закрыта (`done >= goal`); в какие именно дни тренировались — не важно,
    цель задана числом дней. Грейс на текущую неделю сохранён. Оговорка в докстринге:
    `goal` берётся из текущей программы, истории её правок нет, смена программы
    пересуживает прошлые недели.
  - **Пропущенным предлагалась суббота вместо воскресенья.** Порядок перебора был ни
    при чём — он и так идёт от ближайшего. Дело в том, что день закрывался ЛЮБОЙ
    сессией по нему за 7 суток, без оглядки на то, случилась она раньше дня или
    позже: отработанное с опозданием прошлое воскресенье гасило воскресенье
    наступившее. `orm_get_trained_day_ids` (множество id) → `orm_get_last_training_per_day`
    (день → момент последней тренировки), в `missed_day` сравнение даты сессии
    в поясе клиента с датой самого дня. Перенос вперёд (отработал понедельник
    во вторник) при этом продолжает работать.
  - **Возврат на главную «забывал» тренировку** — а вот это не баг: проверяли на
    ПРОДЕ, где крутится образ от 31 июля без объекта `active`. Правка от 2026-08-02
    лежала незакоммиченной. Выкатка её и чинит.
  - **Хвост heatmap.** Сервер отдаёт дни строго по сегодняшний, сетка течёт по
    колонкам на 7 строк — в понедельник последняя колонка состояла из одного
    квадрата, да ещё обведённого как «сегодня», и висела над пустотой. Недостающие
    клетки дорисовывает клиент (`.future`, прозрачный фон — заливка `--heat-0`
    означала бы «отдыхал»); в ответе API таким клеткам делать нечего, это геометрия.
    Заодно `sessions` → `active_days`: подпись стоит над сеткой, а клетка — это день,
    и «19 тренировок» над 18 квадратами читалось как сбой. `max_sets` убран.
  - Тестов 44 → 51. Новые: серия по закрытым неделям, серия вне расписания,
    тренировка раньше своего дня, два теста на календарь активности. Помощник
    `_backdate_to()` ставит тренировке конкретную дату — сдвигом на целые сутки
    разные даты внутри одной недели не набрать.
  - **Доки приведены в порядок**: из корневого README убран LSTM/press_api/PyTorch
    (стека нет с 31 июля), из `miniapp/README` — `predictor.py`/`neiro_api.py`,
    в ONBOARDING поправлены счётчики строк и тестов, §8 больше не говорит «ветка не
    влита», в §10 закрытые вопросы отделены от открытых. `docker-compose.yml` собирал
    удалённый `press_api/Dockerfile`, то есть был сломан, — сервис `press-api` убран.
  - Глазами не проверено: браузера на машине нет. Тест-под правки Python подхватит
    только после `kubectl rollout restart deploy/gym-miniapp-test -n gym-prod`.

- **2026-08-11** — UX сборки программы для новичка. Мерил путь по коду: пусто →
  программа на 3 дня по 4 упражнения = **56 тапов**, из них 12 — повторный ответ на
  вопрос «обычное или круговое» и 12 — повторный проход «Добавить → категория»,
  потому что каждое добавление уводило обратно в день. Сверился с Hevy (пикер
  с поиском и мультивыбором, создание своего упражнения внутри пикера) и с общим
  правилом индустрии: чистый лист новичку не дают нигде.
  - **Пачка.** `POST /api/days/{id}/exercises/batch` — упражнения отмечаются
    галочками и кладутся одним запросом. Последовательно на сервере, а не
    параллельными запросами с клиента: порядок определяет структуру тренировки
    (подряд идущие круговые = один круг), а параллельные завершаются как придётся.
  - **Модалка «Как выполнять?» убрана.** Круг стал общим тумблером на пачку —
    поодиночке круговой блок всё равно не задаётся. Точечно флаг правится там же,
    где и был, на экране упражнения; раньше один факт вводился дважды.
  - **Шаблоны программ** — `miniapp/program_templates.py`, три штуки (всё тело 3 дня,
    жим/тяга/ноги, верх/низ 4 дня). Заданы НАЗВАНИЯМИ упражнений, не id: каталог
    насыпает seed.py при старте, id зависят от порядка вставки и разошлись бы между
    контурами. Разворачивает сервер в обработчике создания — программа появляется
    целиком или никак. Неизвестный id шаблона → 404, иначе пустая программа выглядела
    бы как своя.
  - **Поиск по каталогу.** `/api/catalog` отдаёт ещё и плоский список всех упражнений,
    клиент ищет в памяти вкладки: круг до пода 50–90 мс, на каждую букву туда ходить
    нельзя. Ищет сразу по всем группам — помнить, «жим стоя» это дельты или грудь,
    пользователь не обязан.
  - **Своё упражнение больше не теряет день.** `/my-exercises/:dayId`, созданное
    сразу кладётся в тот день, откуда пришли. Раньше `#mine` уходил без dayId,
    и добавить только что созданное можно было только вернувшись в каталог.
  - **Подходы правятся в строке дня** шторкой с двумя степперами, без ухода на
    отдельный экран. **Круговой блок нарисован блоком** — правило «объединяет
    соседство» до сих пор нигде на экране не было видно.
  - **`Трап.` → `Трапеции`.** Обрезок читался как съехавшая вёрстка.
    `orm_create_categories` заполняет таблицу только пока она пуста, поэтому живую
    базу иначе не поправить — переименование идёт идемпотентно в `seed_catalog`
    при старте. Отдельный тест гоняет именно этот путь (вернуть старое имя → сев).
  - **С главной «Создать программу»** открывает форму, а не список программ
    с такой же кнопкой внутри.
  - Тестов 51 → 57. Глазами не проверено — проверяет пользователь в тест-контуре.

- **2026-08-11** — Правки по фидбеку на экран дня.
  - **Формулировки.** «нажмите, чтобы настроить» под каждым упражнением убрано —
    подпись повторялась на всех строках и не несла ничего. «3 упражнения в круге»
    → «3 круга»: число упражнений видно по строкам, а вот сколько раз проходить
    круг — не видно нигде. Заодно вычищены мои же многословия в шторках создания
    программы, каталоге и своих упражнениях.
  - **Круги правятся тапом по шапке блока.** `/api/day/{id}` теперь отдаёт
    `program.circular_rounds`. ВАЖНО: это настройка ПРОГРАММЫ, `build_plan`
    разворачивает по ней все круговые блоки дня — собственного числа у блока
    в схеме нет. В шторке так и написано; по-настоящему поблочные круги требуют
    колонки у `Exercise` и миграции.
  - **Перетаскивание вместо стрелок ↑↓.** Тянем ЗА РУЧКУ, а не за строку: жест по
    строке спорил бы с прокруткой и потребовал бы длинного нажатия, то есть паузы
    перед каждым перетаскиванием. Место высадки показывает линия, а не
    расступающиеся строки, — строки лежат в разных контейнерах (круговой блок
    оборачивает свои), и раздвигать их пришлось бы с оглядкой на высоту шапки.
    У края экрана список подкручивается сам. Смещение считается в координатах
    СТРАНИЦЫ, иначе автопрокрутка вытаскивала бы строку из-под пальца.
  - Ручка помечена `data-swipe="off"`, и `swipe.js` такие элементы пропускает:
    иначе старт вертикального жеста взводил бы отслеживание листания разделов.
  - **Порядок сохраняется целиком** — `PATCH /api/days/{id}/exercises/order`,
    `orm_reorder_exercises`. Неполный или с дублями список отвергается: разложив
    по нему позиции, мы оставили бы недосланные упражнения на старых местах.
  - Тестов 57 → 60.

- **2026-08-11** — Перетаскивание доведено: строки разъезжаются, задержки нет.
  Было: линия-указатель во время жеста и перерисовка с сервера на отпускании —
  названия менялись местами уже ПОСЛЕ того, как палец отпустил, с задержкой
  на сетевой круг.
  - **Перестановка идёт под пальцем.** Перевалила середину соседа — поменялись
    местами в разметке сразу, соседи доезжают анимацией. Порядок уходит на сервер
    молча вдогонку, экран его не ждёт; при ошибке перерисовываемся от сервера.
  - **Соседей двигаем FLIP'ом** (замерили → переставили → замерили → проиграли
    разницу), а не сдвигом на высоту строки. Сдвиг промахивался бы: строка,
    покидающая круговой блок, меняет высоту блока — у него появляется или
    исчезает шапка.
  - **Список стал ПЛОСКИМ.** Обёртка `.circuit` вокруг круговых убрана: строка,
    уходящая из круга, меняла бы родителя посреди жеста. Теперь блок — это полоса
    слева на строках плюс шапка-сосед, а принадлежность пересчитывает `decorate()`
    по соседям тем же правилом, что и `build_plan` на сервере. Побочный выигрыш:
    вытащил круговое из круга — блок перестроился сразу, а не после перезагрузки.
  - Поднятие снимается по таймеру, а не по `transitionend`: если палец не сдвинул
    строку ни на пиксель, гасить нечего, перехода нет — и события тоже, а строка
    осталась бы висеть с тенью.

- **2026-08-11** — Снаряд упражнения и шаг веса. Шаг кнопок «−/+» был константой
  `2.5` прямо в `workout.js`: на блоке с плитками по 5 такой вес не набрать вовсе,
  на махах гантелями 8 кг это прибавка в треть, а на планке поле веса стояло вообще
  без повода. Разбирался с этим ради подсказок «повысить/понизить вес» — без сетки
  доступных весов они предлагали бы несуществующие числа.
  - **Полем стал снаряд, а не шаг.** `barbell / dumbbell / stack / machine /
    bodyweight / other` на карточках каталога (`admin_exercises`, `user_exercises`),
    шаг считается из него — `services/equipment.py`, модуль чистый, как `workout.py`.
    У пресетов снаряд проставлен нами в `seed.py`: по названию он очевиден, а лишний
    вопрос на пути к первой тренировке дороже точности. Спрашиваем ровно в одном
    месте — в форме своего упражнения.
  - **На `exercise` снаряд лежит СНИМКОМ** — как `name` и `description` рядом.
    Читать его через `admin_exercise`/`user_exercise` честнее, но связи ленивые:
    обращение вне загруженной сессии роняет асинхронный запрос, а упражнения дня
    отдаются из полудюжины мест. NULL там значит «снимок не снят» — по нему
    `seed_catalog` и находит, что досыпать, поэтому у колонки нет server_default.
  - **Догонялка живой базы.** Миграция может дать существующим строкам только
    `'other'` (шаг 2.5, ровно старое поведение): каталог наполняется кодом, id
    пресета на проде и на тесте разные. Правильные значения проставляет
    `seed_catalog` при старте по названию — тот же путь, что у «Трап.» → «Трапеции».
    Плюс второй проход: снимки на уже разложенных по дням упражнениях, коррелированным
    подзапросом (`UPDATE ... FROM` разошёлся бы между Postgres и SQLite тестов).
    Проверено вживую: `alembic upgrade` на реальной SQLite, сев, повторный сев,
    `downgrade`, снова `upgrade`.
  - **Свой вес** (`bodyweight`): подтягивания, брусья, планка, пресс — 6 из 30
    пресетов. Поле веса там СВЁРНУТО, а не убрано: первая версия убирала его
    совсем, и подтягивания с блином на поясе записать было нечем. Разворачивает
    его строка «+ отягощение», и дальше оно открывается само — если вес был
    в прошлый раз или уже записан в этой тренировке (`beltOn` живёт в памяти
    вкладки, подходы — на сервере, поэтому проверяются оба). Строки читаются
    «12 раз» без пояса и «+10 кг × 12» с ним, по весу САМОГО подхода: в одном
    упражнении бывает и так, и так. Заодно вычищено то же самое в истории
    (`0 кг × 0` у пропущенного было и до этого) и в итоге тренировки: день из
    турника даёт нулевой объём, и «0 кг поднято» читалось поломкой.
  - **Блок считается БЛОКАМИ, а не килограммами.** Дважды перебирал число (5 →
    2.5), пока не стало ясно, что спор был не о числе, а о единице: у станка
    человек не набирает вес, он втыкает пин в блок и знает его номер. У разных
    станков блоки разные, поэтому любой шаг в килограммах верен для одного зала
    и мимо для соседнего. Теперь на блоке кнопки «−1 / +1», в поле число блоков,
    подпись «БЛОКИ», строки читаются «7 блоков × 12», дельта — «+1 блок».
    В базу по-прежнему уходят килограммы (`блоки × вес блока`): объём, рекорды
    и графики общие для всех снарядов, и пускать в них вторую единицу нельзя.
    Перевод делает клиент, слой данных про блоки не знает вовсе.
  - **Слово тоже было не то.** Сначала назвал единицу «плиткой» — это словарь
    производителя, а не зала. В зале говорят «поставил семь блоков», и так теперь
    и в интерфейсе, и в коде (`blocks()`, `toBlocks()`, `blocksWord()`).
  - **Ошибка в весе блока не портит главного.** Она пропорционально сдвинет
    объём и рекорды, но ЧИСЛО БЛОКОВ — то, что человек воспроизводит в зале, —
    останется точным. Поэтому дефолт (5) безопасен, а поправка живёт в шторке дня.
  - Для остальных снарядов шаг выбран по асимметрии ошибки: промах в мелкую
    сторону стоит лишнего тапа, промах в крупную делает вес НЕДОСТИЖИМЫМ
    (с шагом 5 от 35 не попасть в 37.5). Отсюда тренажёр 2.5, а не 5.
  - **`Exercise.weight_step`** — шаг конкретного зала, поверх снаряда. Правится
    в той же шторке, где подходы и повторения. Ноль означает «как у снаряда»:
    `None` в PATCH уже занят значением «поле не прислали». В списке шагов всегда
    есть текущий — иначе не совпавший ни с чем шаг не выделился бы, браузер
    показал бы первый пункт, и «Готово» молча переписало бы настройку.
  - **Правка снаряда у своего упражнения догоняет копии в днях** — в отличие от
    названия. Название человек подписывает для себя, а снаряд читает интерфейс,
    и правят его ровно затем, чтобы кнопки веса начали ходить как надо.
  - **Наступил на собственные грабли и починил.** Первая версия решала «есть ли
    вес» по `step != null` — и жим гантелей в тест-боте открылся без поля веса.
    Причина не в снаряде: статика монтируется с диска и обновляется сразу, Python
    приезжает только с рестартом пода, а у старого сервера поля `step` НЕТ ВОВСЕ —
    и `!= null` читал отсутствие как «свой вес». Клиент выводил смысловое решение
    из отсутствия числа. Теперь спрашивает про снаряд (`equipment !== 'bodyweight'`),
    а шаг падает на 2.5, если сервер его не назвал: при любом рассинхроне версий
    экран ведёт себя ровно как до всей этой правки. Тест проверяет теперь именно
    снаряд в состоянии тренировки, а не только шаг.
  - **Округления к сетке снаряда НЕТ намеренно.** Кнопки прибавляют шаг и только;
    привязка к сетке испортила бы 41.25 у того, кто носит микроблины, а сказать
    «у меня шаг другой» пока негде. Появится вместе с `Exercise.weight_step`.
  - Тестов 60 → 67. Глазами не проверено — браузера на машине нет.

- **2026-08-12** — Подсказка по весу: двойная прогрессия. То, ради чего затевался
  снаряд, — без сетки достижимых весов подсказка предлагала бы несуществующие
  числа, а на блоке ещё и не ту единицу.
  - **`services/progression.py`** — чистый модуль, как `workout.py` и `equipment.py`:
    план + два прошлых раза → `{action, weight}`. Двойная прогрессия плюс **2-for-2**
    от NSCA: повышаем, только если план закрыт ДВЕ тренировки подряд. Один удачный
    день не повод — иначе вес растёт после каждой выспавшейся ночи.
  - **Прибавка процентом (2.5 %), а не константой**, и округляется ВВЕРХ до сетки
    снаряда. +2.5 кг это 2 % приседа и треть маха гантелями; одно число не может
    быть верным для обоих. На блоке округление даёт ровно «+1 блок».
  - **Деload** — 10 % после двух проваленных подряд, обязательно НИЖЕ рабочего:
    на 20 кг со ступенью 5 сброс даёт 18, округляется обратно в 20, и «понизить»
    не понизило бы ничего. Провалом считается недобор больше двух повторений
    или подходов меньше плана.
  - **Пропуск ловится по количеству подходов.** В «прошлом разе» пропущенных нет —
    их отфильтровал `_lifted()`. Пришло два вместо трёх → план не закрыт. Без
    этого уход с упражнения читался бы как безупречная тренировка.
  - **Позапрошлый раз** — `orm_get_prev_sets_by_identity(back=2)`. Понадобился
    `group_by` по сессии: без него джойн даёт строку на подход, и OFFSET прыгал бы
    внутри одной тренировки вместо перехода к предыдущей.
  - **Отдельной кнопки «принять» НЕТ.** Подсказка — это предзаполненное поле:
    согласие стоит ноль действий, несогласие — один тап по «−». Объяснения тоже
    нет, его роль играет уже существующая строка «+2.5 кг к прошлому разу».
  - **Сегодняшнее сильнее подсказки:** второй подход открывается с весом первого,
    а не снова с предложенным. Подсказка рассуждала о прошлой тренировке, а этот
    вес человек поднял руками полминуты назад.
  - Тестов 67 → 79 (11 чистых на сам модуль + сквозной на стык с БД).
  - **Дальше:** RIR одним тапом — главный недостающий вход. Сейчас «12 повторений»
    одинаково выглядят и когда мог ещё пять, и когда умер.

- **2026-08-15** — Тест-под лежал в CrashLoopBackOff трое суток. Причина ровно та,
  под которую в `engine.py` и писалась проверка: **тестовая БД отставала от кода
  на миграцию**. Тест монтирует рабочий каталог с диска, то есть крутит
  незакоммиченный тред, а там снаряд — `create_db()` отказывался стартовать без
  `equipment`/`weight_step`.
  - **Почему это провисело трое суток и никто не заметил.** Rolling update
    отработал ровно как задумано: новый под не проходил readinessProbe, старый
    под с кодом от 11 августа остался жив и держал трафик. Приложение работало,
    поэтому ничего не кричало — накопились 902 рестарта и
    `progress deadline exceeded`, но снаружи Mini App отвечал. Вывод на будущее:
    здоровье тест-контура нельзя мерить «открывается ли приложение», надо
    смотреть `kubectl rollout status` — он единственный про это и говорил.
  - **Миграцию накатил с хоста** (`d4e6f8a0b2c3` на `gym-miniapp-db`). У тестовой
    БД нет nodeport, а port-forward в этой сессии был заблокирован — оказалось,
    нода маршрутизирует ClusterIP напрямую (`10.43.0.0/16` через `cni0`), так что
    alembic сходил на `10.43.189.226:5432` из `.venv-bot`. Изнутри пода нельзя
    по той же причине, что и на проде: в образе `d8bfbd1` новых файлов миграций
    нет, да и `alembic/` в под не смонтирован — монтируются только `miniapp/`,
    `database/`, `services/`.
  - После старта `seed_catalog` доложил «снаряд проставлен пресетам: 25» и
    «упражнениям в днях: 25» — догонялка живой базы отработала как задумывалась.
    25 строк остались `other`: это пресеты, которых в текущем seed уже нет,
    у них шаг 2.5, то есть прежнее поведение.
  - **Заодно вскрылось, почему прод отставал.** Думали «не выкачено», а на деле
    четыре коммита **никогда не пушились**: `origin/main` стоял на том же
    `4d5a40c`, что и образы прода. CI не ломался — его просто не звали.
  - Выкатка: 79 тестов зелёные, рабочий тред собран одним коммитом (снаряд
    и прогрессия переплетены в `workout.js` и `test_miniapp_api.py` — резать
    по хункам значило бы оставить красный коммит между ними), доки отдельно.
    Миграцию на прод накатывает Job в CI.

- **2026-08-15** — Здоровье из Apple Health сведено с тренировками. Задача звучала
  как «собирать данные с бота», но собирать было нечего: в Postgres уже лежал год
  истории (121 тренировка, 595 подходов с 17 августа 2025). Проблема была в том,
  что здоровье живёт в MongoDB соседнего стека `health-export`, и места, где видно
  оба, не существовало. Поэтому строился не приёмник, а связка.
  - **Метрики переехали к тренировкам, а не наоборот.** Они плоский временной ряд
    и в Postgres ложатся, тогда как связи `session → set → exercise` в документную
    базу не переехали бы. Плюс Grafana к MongoDB нативно не ходит вовсе.
  - **Форма документа у метрик РАЗНАЯ**, и это определило схему: у шагов одно
    `qty`, у пульса `Min/Avg/Max`, у сна четыре длительности. Одной колонкой
    `value` три формы не описать, поэтому строка — это пара `name` + `field`
    (`heart_rate/avg`, `sleep_analysis/deep`). Обычный длинный формат временных
    рядов, он же ровно то, что Grafana ждёт от запроса.
  - **Числовые поля берутся ОТ ПРОТИВНОГО** — всё, что не служебное и приводится
    к числу. Перечислять поимённо значило бы молча терять метрики: их под сотню,
    и появляются они по мере того, как человек включает их в выгрузке.
  - **Уникальный ключ — суть затеи.** Health Auto Export шлёт перекрывающиеся окна,
    один день приезжает при каждой синхронизации; ключ превращает повтор в апсерт.
    `source` в ключе обязателен: 19 июня шаги пришли дважды — 3270 с телефона и
    5200 с часов. И он NOT NULL с пустой строкой по умолчанию, потому что NULL
    в уникальном ключе не равен сам себе и документ без устройства дублировался бы
    при каждом прогоне, а заметить это было бы некому.
  - **Грабли SQLite:** `BIGINT PRIMARY KEY` там не автоинкрементится, только
    `INTEGER PRIMARY KEY`. Лечится `with_variant(Integer, "sqlite")`. И `constraint=`
    в `on_conflict_do_update` понимает лишь Postgres — конфликт описан колонками.
  - **Апстрим уже всё умел, и это выяснилось поздно.** hae-server — это
    `HealthyApps/health-auto-export-server`, и он везёт готовые дашборды Grafana,
    работающие через плагин **Infinity** прямо по REST API, без всякой нашей базы.
    Свои панели для сна и шагов оказались лишней работой и были выкинуты: наш
    дашборд оставлен только на то, чего апстрим структурно не может, — связь
    здоровья с ТРЕНИРОВКАМИ. Два его дашборда зашивают uid источника
    `bee4k1otx5urkf`, поэтому Infinity заведён ровно с ним.
  - **Заодно вскрылась моя ошибка в комментариях:** я написал, что у API нет
    фильтра по дате. Есть — `?from=&to=` в epoch-миллисекундах, именно так его
    зовут дашборды апстрима. Полный проход оставлен сознательно (самоисцеляется,
    14 секунд на 31 тысячу строк), но обоснование пришлось переписать на честное.
  - **Грабли Grafana 13:** в провижининге тип источника Postgres — это
    `grafana-postgresql-datasource`. Короткое `postgres` осталось в plugin.json
    только как `aliasIDs`, и провижининг его НЕ разрешает. Диагностика была
    неочевидной: источник просто не создавался, **без единой строки в логе**,
    все панели показывали «No data» с красным треугольником, а к Postgres не
    уходило ни одного запроса — то есть падало ДО запроса. Нашлось сравнением:
    в логе была строка `inserting datasource` про Infinity и не было про GYM.
  - Grafana читает Postgres ролью `grafana` только с `SELECT` (проверено:
    `INSERT` даёт `InsufficientPrivilegeError`). Манифесты `k8s/grafana-*.yaml`
    накатываются **руками**: CI деплоит в `gym-prod`, а Grafana живёт
    в `monitoring`.
  - Тестов 79 → 91. Бэкфилл: 31 091 строка с 19 июня 2026.

- **2026-08-15** — Бот слал админу пачку «Загружен баннер: …» на каждом старте.
  Заметилось потому, что за день было четыре выкатки, и пачка пришла четырежды.
  - **Первопричина — опечатка в имени файла.** На диске лежал `errror.png`
    (три «р»), а код читает баннер `error`. `orm_change_banner_image` делает
    `UPDATE ... WHERE name='errror'`, такой строки нет, апдейт молча меняет ноль
    строк — и `file_id` не сохраняется НИКОГДА. Отсюда же вечно пустой
    `globals.error_pic`: картинки ошибки у бота не было вовсе.
  - **Усугубляло отсутствие проверки:** заливка не смотрела, добыт ли уже
    `file_id`, и перезаливала все восемь картинок каждый старт. А `file_id`
    не протухает — он привязан к боту, и одного раза достаточно.
  - Сделано: файл переименован; заливка пропускает баннеры с `file_id` и
    ПРОПУСКАЕТ картинки, под которые нет строки в базе (слать их бессмысленно —
    результат некуда сохранить); `orm_change_banner_image` возвращает rowcount,
    чтобы такая опечатка больше не молчала; порядок в `on_startup` исправлен —
    `error_pic` читается ПОСЛЕ заливки, иначе в запуск, когда картинка наконец
    добудется, переменная осталась бы пустой.
  - **Сообщение «Бот запущен в режиме POLLING» убрано под флаг**
    `NOTIFY_ADMINS_ON_START` (по умолчанию выключено). Оно писалось на каждый
    старт процесса, а живость и так видит readinessProbe через `/healthz`.
  - Тестов 91 → 95. Осталось незакрытым: пуш, меняющий только доки, всё равно
    гоняет полный пайплайн и катит боевого бота — лечится `paths-ignore`
    в `deploy.yaml`.

- **2026-08-19** — Разбор боевых данных и чистка каталога. Началось с «посмотри мою
  программу», а кончилось тем, что каталог год стоял разъехавшимся с кодом.
  - **Каталог не совпадал с `seed.py` по двум осям, и обе молчали.**
    (1) Категории в живой базе зовутся «Грудные» и «Трапеция», а `CATALOG` ищет
    «Грудь» и «Трапеции»; проверка `category not in categories` пропускает молча,
    поэтому **на прод не заехал ни один пресет груди** и «Шраги со штангой» —
    год каталог выглядел полным, а половины груди в нём не было.
    (2) Буква **«ё»**: в базе «Жим штанги лежа» и «Подъем штанги на бицепс»,
    в коде «лёжа» и «Подъём». Сверка идёт по имени, поэтому вместо обновления
    заводился дубль, а старой карточке снаряд не проставлялся НИКОГДА — отсюда
    те самые 25 пресетов с `equipment='other'`, списанные 15 августа на
    «их нет в текущем seed».
  - Сделано: `RENAMED_CATEGORIES` пополнен обоими именами; заведён
    `MERGED_EXERCISES` (13 пар) и шаг `_merge_duplicates`; `_sync_equipment`
    превратился в `_sync_cards` и чинит заодно категорию карточки («Жим гантелей
    лёжа» лежал в «Прессе», «Становая тяга» — в «Ногах»). `CATALOG` дополнен
    тем, что реально живёт в базе (болгарские, разгибания/сгибания ног, пресс
    в тренажёре, кроссовер, гиперэкстензия, Т-гриф и прочее) — иначе снаряд им
    не проставится по той же причине, что и раньше.
  - **Порядок шагов в `seed_catalog` критичен**: категории → слияние двойников →
    досыпка → синхронизация. Слияние обязано идти ДО досыпки, иначе канонические
    имена добавились бы третьими карточками рядом с обеими старыми. А внутри
    слияния перецепка `exercise.admin_exercise_id` обязана идти ДО удаления
    старой карточки: там `ON DELETE CASCADE`, и у `exercise → set` тоже, то есть
    удаление первым унесло бы год подходов.
  - Тестов 95 → 99: схлопывание двойника с сохранением подходов, переименование
    по «ё» с сохранением id карточки, разблокировка пресетов за переименованной
    категорией, переезд пресета в свою группу.
  - **Данные пользователя 851690283 за год** (пригодятся, если снова спросит):
    69 тренировок за 366 дней = 1.3 в неделю, апрель–май 2026 пропущены целиком;
    19 подходов в неделю на всё тело при норме 10–20 на мышцу; спина 1.8/нед,
    ноги 0 с 4 июля. Расчётный 1ПМ по лифтам с историей: молотки 29.3 → 29.3
    за 12 месяцев, бицепс 63.3 → 63.3 за 6.5, жим в наклоне 46.7 → 46.7 за 6.
    Растут только подтягивания (20 → 26) и разведения (там сменилась техника —
    стоя на сидя, и веса до/после несравнимы).
  - **Дыра, которую это вскрыло:** записать смену техники негде. `note` есть
    только у сессии, у упражнения ничего, а `suggest()` сравнивает по идентичности
    упражнения. Переход на строгую технику с тем же весом = два недобора подряд =
    `_failed` дважды = предложение СБРОСИТЬ вес, то есть ровно наоборот. Лечится
    либо вариантами упражнения с раздельной историей, либо кнопкой «сбросить
    историю прогрессии».
  - Программа «Гипертрофия» (верх/низ 4 дня) собрана скриптом, но **накатить
    её не удалось**: классификатор разрешений режет запись в прод-базу через
    `kubectl exec ... psql`. Скрипт отдан пользователю руками. Читать базу при
    этом можно — весь разбор выше сделан чтением.

- **2026-08-21** — Первая тренировка по новой программе и правка её состава.
  Читалось всё из прод-базы; писал в неё пользователь руками — скрипт отдан ему.
  - **Верхние дни собирались без оглядки на то, что человек реально делает.**
    Из программы выпали **брусья** (12 подходов истории, растут: 3 авг 40×10 →
    16 авг 45×10) и **пресс в тренажёре** (180 подходов, самое частое движение
    за год) — оба вернулись. На месте брусьев стоял жим гантелей лёжа, движение
    без единого подхода в истории: на дефиците менять то, где растёшь, на то, где
    заново искать вес, — плохой размен.
  - **План по ВЕРХУ диапазона превращается в предложение сбросить вес.** Это
    прямое столкновение методики с `services/progression.py`: `_failed` считает
    провалом недобор больше двух повторений ДО ПЛАНА, а двойная прогрессия ровно
    в том и состоит, что недели идут ниже цели. На живых числах: махи 17.5 шли
    10,10 → 11,11,11 → 12,12,12 (образцовый рост), при плане 4×15 приложение
    предложило бы **понизить** до 15 кг. Лечение сегодня — ставить в план то,
    что человек уже закрывает. Лечение по-хорошему — считать провалом откат
    относительно ПРОШЛОГО РАЗА, а не недобор до плана; тогда же чинится и «ушёл
    из зала на 3 подходе из 4», который сейчас неотличим от «не смог».
  - **Все четвёрки убраны.** По истории делается 3 подхода лесенкой вниз
    (45×10, 45×8, 40×10), а план в 4 подхода `_cleared` не закрывает никогда:
    повышения не будет, зато два раза подряд сработает деload.
  - **Объём не влезал во время.** Фактический темп по меткам записи — 5–8 минут
    на подход, типичная тренировка 9–12 подходов за 40–60 минут. Вторник на 20
    подходов это два часа, суббота на 24 — два с половиной. Стало 15/13/18/18,
    64 подхода в неделю против 79.
  - **Единицы блочных тренажёров — не трогали намеренно.** Пресс, разгибания
    и сгибания ног человек пишет номерами блоков (12–13, 4–6). Снаряд `stack`
    показывал бы номер блока, а в базу писал `блоки × 5`, и 13 превратилось бы
    в 65 — рекорд и график разорвались бы посередине. Поэтому `machine`
    с `weight_step = 1`: кнопки ходят по одному блоку, число хранится тем же.
  - **Жиму гантелей в наклоне задан `weight_step = 5`** — гантель 37.5 в зале
    потеряна, и прибавка обязана вести с 35 сразу на 40.
  - **Прогон вхолостую поймал грабли**: скрипт исполнялся на тестовой базе
    с копией программы и откатом, и упал на дубликате ключа — строки, залитые
    с явными id, оставляют `exercise_id_seq` позади. На проде она в порядке,
    но `setval` в скрипт добавлен.
  - Слияние двойников каталога выкачено этим же днём (13 пар, самая дорогая —
    108 подходов «Разведения гантелей» → «Махи гантелями в стороны»,
    подтверждено пользователем). До выкатки новая программа смотрела на пустые
    карточки: молотки, махи и бицепс открывались бы в зале без «прошлого раза»,
    рекорда и подсказки.
  - Данные тренировки 20.08 оставлены как есть: три подхода разгибаний ног,
    сделанных вместо сгибаний (потянута связка), и забытый третий подход
    на носки в базу не дописаны — пользователь решил не добирать.

## Заметки на будущее
- Живой `gym-miniapp-test` отстал от манифеста: в спеке ещё висит `NEIRO_API_URL`,
  хотя из `k8s/` он убран. Лечится повторным `kubectl apply` (перезапустит под).
